import styled from 'styled-components'

export const IconText = styled.h1`
  color: ${props => (props.islight ? '#00306e' : '#475569')};
  font-size: 15px;
  font-weight: 600;
`
export const Head = styled.h1`
  color: ${props => (props.islight ? '#00306e' : '#475569')};
  font-size: 18px;
  font-weight: 600;
`
export const Para = styled.p`
  color: ${props => (props.islight ? '#00306e' : '#475569')};
  font-size: 10px;
  font-weight: 400;
`
